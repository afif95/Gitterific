# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page#
GET     /index                           controllers.ApiController.index1

GET	   /searchQuery/:key                 controllers.ApiController.searchQuery(key: String)

GET 	/home						controllers.ApiController.home(request: Request)
+nocsrf
POST 	/home						controllers.ApiController.save(request: Request)

GET     /                           controllers.HomeController.index
GET    /explore                     controllers.HomeController.explore
GET    /tutorial                    controllers.HomeController.tutorial
GET    /time                    	controllers.HomeController.time


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
